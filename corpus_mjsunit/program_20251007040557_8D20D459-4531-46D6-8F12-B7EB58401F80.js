function reduceLHS() {
    for (let i2 = 0; i2 < 2; i2++) {
        let  = [1,1];
        r = r - 1;
        q += 1;
        q = r;
    }
}
try { reduceLHS(); } catch (e) {}
try { reduceLHS(); } catch (e) {}
function reduceRHS() {
    for (let i22 = 0; i22 < 2; i22++) {
        let  = [1,1];
        r = 1 - r;
        q += 1;
        q = r;
    }
}
try { reduceRHS(); } catch (e) {}
try { reduceRHS(); } catch (e) {}
