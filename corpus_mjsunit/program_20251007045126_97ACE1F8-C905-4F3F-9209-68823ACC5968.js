var zero = 0;
var one = 1;
var minus_one = -1;
var two = 2;
var four = 4;
var minus_two = -2;
var minus_four = -4;
-Infinity;
one / -zero;
-Infinity;
one / (zero * minus_one);
-Infinity;
one / (minus_one * zero);
one / (zero * zero);
one / (minus_one * minus_one);
-Infinity;
one / (zero / minus_one);
one / (zero / one);
-Infinity;
one / (minus_four % two);
-Infinity;
one / (minus_four % minus_two);
one / (four % two);
one / (four % minus_two);
-Infinity;
one / (0 * minus_one);
-Infinity;
one / (-1 * zero);
one / (0 * zero);
one / (-1 * minus_one);
-Infinity;
one / (0 / minus_one);
one / (0 / one);
-Infinity;
one / (-4 % two);
-Infinity;
one / (-4 % minus_two);
one / (4 % two);
one / (4 % minus_two);
-Infinity;
one / (zero * -1);
-Infinity;
one / (minus_one * 0);
one / (zero * 0);
one / (minus_one * -1);
-Infinity;
one / (zero / -1);
one / (zero / 1);
-Infinity;
one / (minus_four % 2);
-Infinity;
one / (minus_four % -2);
one / (four % 2);
one / (four % -2);
-Infinity;
one / -0;
-Infinity;
one / (0 * -1);
-Infinity;
one / (-1 * 0);
one / (0 * 0);
one / (-1 * -1);
-Infinity;
one / (0 / -1);
one / (0 / 1);
-Infinity;
one / (-4 % 2);
-Infinity;
one / (-4 % -2);
one / (4 % 2);
one / (4 % -2);
x = 0;
z = 3044;
function foo(a234) {
    var y = -a234 + z;
    return -a234;
}
-0;
foo(x);
-0;
foo(x);
